import { EmailTemplateMaker } from "@/screens/emailTemplateMaker/EmailTemplateMaker";
import Layout from "@/screens/layout/Layout";
import React from "react";

const page = () => {
  return (
    <Layout>
      <EmailTemplateMaker />
    </Layout>
  );
};

export default page;
